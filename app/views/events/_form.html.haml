= form_for @event do |f|
  -if @event.errors.any?
    #error_explanation
      %h2= "#{pluralize(@event.errors.count, "error")} prohibited this event from being saved:"
      %ul
        - @event.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :program_id
    = f.number_field :program_id
  .field
    = f.label :event_status
    = f.text_field :event_status
  .field
    = f.label :event_is_program
    = f.text_field :event_is_program
  .field
    = f.label :event_name
    = f.text_field :event_name
  .field
    = f.label :rating
    = f.text_field :rating
  .field
    = f.label :pricing
    = f.text_area :pricing
  .field
    = f.label :description
    = f.text_area :description
  .field
    = f.label :trip_info
    = f.text_area :trip_info
  .field
    = f.label :gear_list
    = f.text_area :gear_list
  .field
    = f.label :confirmation_page
    = f.text_area :confirmation_page
  .field
    = f.label :question1
    = f.text_area :question1
  .field
    = f.label :start_date
    = f.date_select :start_date
  .field
    = f.label :end_date
    = f.date_select :end_date
  .actions
    = f.submit 'Save'
