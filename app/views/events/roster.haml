= render 'left_tabs'
#right

  %p the Roster!!!

  %p This is event:

  = @event.id



  %table
    %tr
      %th Name
      %th Experience
      %th Exercise
      %th Medical
      %th Diet
      %th Register status
      %th Gear answer
      %th Answer1
      %th Has questions
      %th Event ID
      %th
      %th

    - Registration.where(:event_id => @event.id).each do |registration|
      %tr
        = form_tag(:registration, :method => :put, :remote => true) do
          %td= registration.user.first_name
          %td= registration.user.experience
          %td= registration.user.exercise
          %td= registration.user.medical
          %td= registration.user.diet
          %td= registration.register_status
          %td= registration.gear_answer
          %td= registration.answer1
          %td= registration.has_questions
          %td= registration.event_id
          %td= hidden_field_tag :event_id, @event.id
          %td= hidden_field_tag :user_id, registration.user.id
          %td= hidden_field_tag :new_status, "new_status"
          %td= submit_tag 'Approved'
          %td= submit_tag 'Wait List'
          %td= submit_tag 'Canceled'
          %td= submit_tag 'Registrar'
          %td= submit_tag 'Leader'
          %td= submit_tag 'Co-Leader'

  =link_to "Export as Excel", roster_export_url
